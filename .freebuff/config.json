{
  "install": {
    "command": "pip install -r requirements.txt",
    "description": "Install Python dependencies (numpy, pandas, matplotlib, seaborn, scipy, jupyter)"
  },
  "preview": {
    "command": "python -m src.main",
    "description": "Run the full Spotify Trends Analysis EDA pipeline (load → inspect → clean → analyze → visualize)"
  },
  "dev": {
    "command": "python -m src.main --skip-visualization",
    "description": "Run analysis pipeline without generating visualizations"
  },
  "build": {
    "command": "python -m src.main --input data/spotify.csv --output-dir images",
    "description": "Run full pipeline with explicit input/output paths"
  }
}
